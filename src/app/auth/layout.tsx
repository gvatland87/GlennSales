/**
 * Auth-layout — ingen Navbar, ingen padding-container.
 * Logg-inn, registrer og profil-oppsett har sin egen fullskjerm-layout.
 */
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
